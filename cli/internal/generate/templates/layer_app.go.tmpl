// Package application holds the {{.Title}} feature's use cases. A use case
// is an app.Handler: one request in, one response out, and an error from
// the warren/errors vocabulary.
//
// It imports no transport package — that is the entire point.
package application
