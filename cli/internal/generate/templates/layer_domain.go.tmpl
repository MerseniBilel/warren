// Package domain is the {{.Title}} feature's domain layer: entities, value
// objects, events, and the repository ports.
//
// It imports nothing from application, infrastructure, or interfaces. That
// rule is what `warren lint arch` enforces, and it is why this layer can be
// tested, versioned, and extracted on its own.
package domain
