// Package infrastructure holds the {{.Title}} feature's driver-facing code:
// the repository implementations, the clients, the mappers.
//
// It depends on the domain. The domain never depends on it.
package infrastructure
