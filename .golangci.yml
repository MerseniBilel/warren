version: "2"

linters:
  default: standard
  enable:
    - gocritic
    - misspell
    - revive
    - unconvert
    - unparam
  settings:
    revive:
      rules:
        - name: exported # every exported identifier gets a doc comment starting with its name
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - unparam
          - revive # test fixtures need no doc comments; the §3.1 example stays verbatim

formatters:
  enable:
    - gofmt
    - goimports
